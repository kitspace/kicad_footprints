#!/usr/bin/env bash
set -eu
set -o pipefail

git submodule | awk '{ print $2 }' | xargs -i --max-procs=10 bash -c "echo 'Checking' {} && cd {} && git fetch --quiet && git reset --quiet --hard origin/HEAD || echo 'Failed: ' {}"
